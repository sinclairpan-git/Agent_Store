# Verification Rules

Verification profile markers used by `ai-sdlc verify constraints`:

- docs-only
- rules-only
- truth-only
- code-change
- `uv run ai-sdlc verify constraints`
- `python -m ai_sdlc program truth sync --dry-run`
- `uv run pytest`
- `uv run ruff check`
